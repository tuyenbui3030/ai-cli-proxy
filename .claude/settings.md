# Claude Code Settings

## Memory

Project: AI CLI Proxy Service
Purpose: Docker-based proxy for multiple AI providers (Gemini, Claude, Codex)
Language: Shell scripts, YAML configuration, Docker

## Preferences

- Use `docker compose` (not `docker-compose`) for Docker commands
- Default timezone: Asia/Ho_Chi_Minh
- Config files should never be committed to git
- Always validate YAML syntax before saving config changes
